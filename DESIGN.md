## Overview

개미반대로(Ant-Contra)의 비주얼 시스템은 **애플 리퀴드 글래스(Apple Liquid Glass) 문법**을 따른다 — 연한 블루-그레이 캔버스(`#F2F5F9`) 위에 반투명 유리 패널(`rgba(255,255,255,0.55~0.65)` + `backdrop-filter: blur(16~24px) saturate(180%)`)을 겹겹이 띄우고, 상단만 밝게 처리한 하이라이트 보더(`rgba(255,255,255,0.85~0.9)`)와 다층 그림자로 깊이감을 만드는 반투명·입체 디자인이다. 배경에는 오렌지·블루·퍼플 radial 그라디언트를 옅게 고정(`background-attachment: fixed`) 배치해 유리 재질에 주변광이 은은하게 비치는 느낌을 준다. 타이포는 Pretendard Variable(본문)과 JetBrains Mono(모듈 경로·TTL 등 기술 라벨)를 조합하고, 국내 증권 관행에 따라 상승은 빨강 하락은 파랑을 유지한다. 카드는 이전보다 큰 라운드(16~24px)와 hover 시 `translateY(-3px)` + 그림자 확장으로 유리가 살짝 들리는 촉각적 반응을 준다.

**핵심 특징 (v2 — Liquid Glass)**

- 연한 블루-그레이 캔버스(`#F2F5F9`) + 다층 radial 그라디언트 앰비언트 라이트(오렌지 6~10% · 블루 8~12% · 퍼플 6%), `fixed` 고정
- 반투명 유리 패널: `rgba(255,255,255,0.55~0.65)` 배경 + `backdrop-filter: blur(16~24px) saturate(180%)`, 상단 hairline만 `rgba(255,255,255,0.85~0.9)`로 밝게 처리해 반사광 표현
- 상승=빨강 / 하락=파랑 원칙은 그대로 유지, 화살표·텍스트 항상 병기(색맹 대응)
- 상태/신뢰도 배지는 solid fill 대신 **반투명 틴트 배경 + 틴트 보더** 조합으로 전환 (예: 초록 텍스트 위 `rgba(220,252,231,.7)` 배경)
- Pretendard Variable 단일 서체 + JetBrains Mono는 **모듈 경로/수치/코드성 라벨 전용** — 서술형 본문에는 쓰지 않음
- 라운드는 전반적으로 확대(12~24px), 배지·태그는 fully rounded 유지
- Hover는 항상 `translateY(-3px)` + `box-shadow` 확장 + `background` 밝기 상승(hover 전용 변수) 세트로 동반, 단독 사용 금지

## Colors

### Base Canvas & Glass Surface

- **Background** (`#F2F5F9`): 앱 전체 캔버스.
- **Ambient Gradient**: 좌상단 오렌지 `rgba(255,107,0,0.10)`, 우상단 블루 `rgba(37,99,235,0.12)`, 하단중앙 퍼플 `rgba(147,51,234,0.06)` — `background-attachment: fixed`로 스크롤에도 고정.
- **Glass Surface (기본)**: `rgba(255,255,255,0.55~0.65)`
- **Glass Surface (hover)**: `rgba(255,255,255,0.82)`
- **Glass Border**: `rgba(255,255,255,0.7)` 기본, 상단 보더만 `rgba(255,255,255,0.85~0.9)`로 하이라이트
- **Glass Border (subtle, 구분선용)**: `rgba(226,232,240,0.6)`
- **Glass Shadow**: `0 16px 36px rgba(15,23,42,0.05), 0 4px 12px rgba(0,0,0,0.02)`
- **Glass Shadow (hover)**: `0 20px 40px rgba(15,23,42,0.09), 0 6px 16px rgba(0,0,0,0.03)`

### Text

- **Text Main** (`#0F172A`): 헤드라인, 가격, 강조 숫자.
- **Text Sub** (`#475569`): 라벨, 보조 설명.
- **Text Faint** (`#94A3B8`): 캡션, 타임스탬프, 경로/코드성 라벨.

### Price / Direction (국내 증권 관행 — 상승 빨강 · 하락 파랑, 유지)

- **Up / 상승** (`#f04452`) — 변경 없음.
- **Down / 하락 / Info Accent** (`#2563EB`): 기존 `#3182f6`에서 톤 정리, 정보성 액센트 겸용.

### Brand Accent

- **Primary Orange** (`#FF6B00`): 기존 `#FF7A2F`보다 채도 낮춘 톤. 로고·VWAP 라인·매매전략 레일에 사용.
- **Accent Soft (배경)**: `rgba(255,107,0,0.1)`
- **Accent Line (보더)**: `rgba(255,107,0,0.3)`

### Status / Tier 색상 (유리 톤 재조정)

3단계 신뢰도/신호 체계를 solid → 반투명 틴트로 전환. 기존 `green/yellow/red` 명명을 `Tier1/Tier2/Tier3`로 확장 사용(지표 신호뿐 아니라 데이터 소스 신뢰도 등급에도 공통 적용).

| 등급                          | Text      | Background              | Border                  |
| ----------------------------- | --------- | ----------------------- | ----------------------- |
| **Tier 1 — 포착/1차 신뢰**    | `#15803D` | `rgba(220,252,231,0.7)` | `rgba(187,247,208,0.9)` |
| **Tier 2 — 중립/2차 폴백**    | `#B45309` | `rgba(254,243,199,0.7)` | `rgba(253,230,138,0.9)` |
| **Tier 3 — 미포착/최종 백업** | `#B91C1C` | `rgba(254,226,226,0.7)` | `rgba(254,202,202,0.9)` |
| **Blue — 정보/시세**          | `#2563EB` | `rgba(239,246,255,0.7)` | —                       |

### Fear / Greed 게이지 5단계, 시장 배지(KOSPI/KOSDAQ), 랭킹 배지

기존 팔레트(하드코딩 solid bg/text) 유지하되, 카드 컨테이너 자체가 유리 패널로 바뀌므로 배지들은 위 Tier 표기법에 맞춰 **틴트 배경**으로 재조정 검토. 게이지 아크 그라디언트(`#4ade80 → #a3e635 → #facc15 → #fb923c → #f87171`)와 바늘 색(`#1e293b`)은 변경 없음.

## Typography

- **본문/제목**: Pretendard Variable (`'Pretendard Variable', 'Pretendard', -apple-system, ...` 폴백)
- **기술 라벨 전용**: JetBrains Mono — 모듈 경로(`price_data.py`), TTL, 수치 배지, 코드성 문자열에만 사용. 서술형 문장·설명 텍스트에는 사용 금지.
- 숫자 위계는 크기보다 굵기(500~900) 차이로 강조하는 기존 원칙 유지.

## Layout & Structure

- **카드 보더 반경**: 일반 카드 `16px`, 섹션/대형 패널 컨테이너 `20~24px`, 배지·태그는 fully rounded 유지 (기존 8~16px 대비 전반 확대).
- **카드 그림자 + Hover**: 기본 `0 16px 36px rgba(15,23,42,.05), 0 4px 12px rgba(0,0,0,.02)` → hover 시 `0 20px 40px rgba(15,23,42,.09), 0 6px 16px rgba(0,0,0,.03)` **+ `translateY(-3px)` + 배경 밝기 상승**을 항상 세트로 적용.
- **유리 카드 적용 범위 확장**: 기존엔 "티커/스캐너 패널 한정"이었으나, 이제 사이드바 카드·지표 신호 카드·문서형 섹션 등 **모든 패널형 컴포넌트의 기본값**으로 승격.
- **Blur 강도**: 낱개 카드 `blur(16px)`, 상위 섹션/컨테이너 `blur(24px)` — 항상 `saturate(180%)` 동반.
- **그리드**: 상세 대시보드 6:4, 홈 스캐너 `repeat(auto-fit, minmax(290px,1fr))`, 반응형 브레이크포인트(900px/550px) 기존 유지.

## Components

### 유리 카드 공통 스펙 (신규 — 전 컴포넌트 기본값)

```
background: rgba(255,255,255,0.55~0.65);
border: 1px solid rgba(255,255,255,0.7);
border-top: 1px solid rgba(255,255,255,0.85~0.9);
backdrop-filter: blur(16~24px) saturate(180%);
box-shadow: 0 16px 36px rgba(15,23,42,.05), 0 4px 12px rgba(0,0,0,.02);
transition: transform .2s ease, box-shadow .2s ease, background .2s ease;
```

### 가격/변동 표시

- 상승·하락은 항상 `▲/▼` + `%` + 색상 3중 표기 원칙 유지.

### 신뢰도/신호 배지 (변경)

- solid 배경 배지 → **Tier 표(위 참조) 기반 rgba 틴트 배경 + 틴트 보더**로 전환.
- 배지 자체에는 blur 적용하지 않음(가독성 우선), 배지를 담는 카드에만 유리 효과 적용.

### 지표 신호 카드 / 손익비 타점 카드 / 스캐너 리스트

- 컨테이너를 유리 카드 공통 스펙으로 전환, 좌측 상태 레일(4px)은 유지하되 레일 색상은 Tier 표 텍스트 컬러 사용.

## Do's and Don'ts

### Do

- 모든 패널형 컴포넌트에 `backdrop-filter: blur() saturate(180%)` 유리 질감을 기본값으로 적용.
- 카드 상단 보더만 더 밝게(`rgba(255,255,255,.85~.9)`) 주어 유리의 상단 반사광을 표현.
- hover는 `translateY(-3px)` + 그림자 확장 + 배경 밝기 상승을 **항상 세트로** 적용.
- 유리 카드는 반드시 고정된 radial 그라디언트 배경 위에서만 배치 — 단색 배경에 얹으면 대비가 사라짐.
- 상승/하락 색상+화살표+텍스트 3중 표기 원칙은 스타일 전환과 무관하게 항상 유지(접근성).

### Don't

- 불투명 흰색(`#FFFFFF` 100%) 카드로 회귀하지 않는다 — 구버전 Toss 플랫 스타일은 폐기.
- 진한 solid 컬러 배지 배경을 쓰지 않는다 — 항상 rgba 틴트 배경 사용.
- 그림자 없이 blur만 단독으로 쓰지 않는다 — 평면적으로 보이므로 다층 그림자를 반드시 동반.
- 크림/올리브 계열 팔레트는 여전히 사용하지 않는다 — 이 프로젝트의 브랜드가 아니다.
- 리퀴드 글래스 효과를 배지 텍스트 자체에는 적용하지 않는다 — 가독성 저하.
